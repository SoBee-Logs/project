# 키 파일 (절대 커밋 금지)
*.pem
*.key
airflow/passwords.json

# 환경변수 (절대 커밋 금지)
.env
*.env
.env.local
.env.*.local

# Spring 설정 (DB 비밀번호, JWT, AWS 키 포함)
application.yml
application.yaml

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.idea/
.vscode/
*.iml

# 로그
*.log
logs/

.venv/
venv/
tmp/

# 로컬 도커 오버라이드 (AWS 배포에 포함되면 안 됨)
docker-compose.override.yml

# Python
__pycache__/
*.pyc
*.pyo

prompts.json

node_modules/

md_docs/